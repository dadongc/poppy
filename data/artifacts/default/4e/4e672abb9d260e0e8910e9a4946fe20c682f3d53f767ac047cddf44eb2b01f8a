ToolResult(call_id='', name='web_search', status='ok', content='1. firecrawl/firecrawl-scrape — Agent Skills | officialskills.sh\n   URL: https://officialskills.sh/firecrawl/skills/firecrawl-scrape\n   # firecrawl-scrape. Extracts clean markdown from any URL, including JavaScript-rendered single-page apps. ## Setup & Installation. npx skills add https://github.com/firecrawl/firecrawl-scrape --skill firecrawl-scrape. or paste the link and ask your coding assistant to install it. https://github.com/firecrawl/firecrawl-scrape. ## What This Skill Does. Extracts clean markdown from any URL, including JavaScript-rendered single-page apps. Supports multiple concurrent URLs and returns content optimized for LLM consumption. Unlike WebFetch, it handles JS-rendered SPAs and scrapes multiple URLs in parallel, so you get usable markdown instead of raw HTML or incomplete content. * Pulling pricing page content from a SaaS site to compare plans. * Extracting docs from a JS-rendered framework site that WebFetch can\'t read. * Scraping multiple product pages concurrently into local markdown files. * Asking a targeted question about a specific webpage without saving the full content. ## Similar Skills. ### pptx-generator. Generates, edits, and reads PowerPoint files using PptxGenJS and markitdown.### wp-project-triage. Writes internal communications in company-specific formats.### pptx.\n\n2. cli/skills/firecrawl-scrape/SKILL.md at main · firecrawl/cli · GitHub\n   URL: https://github.com/firecrawl/cli/blob/main/skills/firecrawl-scrape/SKILL.md\n   You signed in with another tab or window. | description | Extract clean markdown from any URL, including JavaScript-rendered SPAs. Use this skill whenever the user provides a URL and wants its content, says "scrape", "grab", "fetch", "pull", "get the page", "extract from this URL", or "read this webpage". Handles JS-rendered pages, multiple concurrent URLs, and returns LLM-optimized markdown. # Basic markdown extraction # "" " "# Main content only, no nav/footer # "" " "# Wait for JS to render, then scrape # "" " "# Multiple URLs (each saved to .firecrawl/) # # Get markdown and links together # "" " " # Ask a question about the page #"https://example.com/pricing" " " "What is the enterprise plan price?" " ". * **Prefer plain scrape over `--query`.** Scrape to a file, then use `grep`, `head`, or read the markdown directly — you can search and reason over the full content yourself. * firecrawl-interact — when scrape can\'t get the content, use `interact` to click, fill forms, etc.\n\n3. Firecrawl Skills — AI Skill — Termo\n   URL: https://termo.ai/skills/firecrawl-skills\n   ## Launch an agent with Firecrawl Skills. # Firecrawl Skills. Firecrawl CLI for web scraping, crawling, and search. Scrape single pages or entire websites, map site URLs, and search the web with full content extraction. Use the `firecrawl` CLI to fetch and search the web. firecrawl search "your query" -o .firecrawl/search-{query}.json. firecrawl search "your query" --scrape -o .firecrawl/search-{query}-scraped.json. firecrawl scrape https://example.com -o .firecrawl/{site}-{path}.md. firecrawl search "your query" -o .firecrawl/search-query.txt. firecrawl search "your query" -o .firecrawl/search-query.json --json. firecrawl search "AI news" --limit 10 -o .firecrawl/search-ai-news.json --json. firecrawl search "tech startups" --sources news -o .firecrawl/search-news.json --json. firecrawl search "machine learning" --sources web,news,images -o .firecrawl/search-ml.json --json. firecrawl search "web scraping python" --categories github -o .firecrawl/search-github.json --json. firecrawl search "firecrawl tutorials" --scrape -o .firecrawl/search-scraped.json --json. firecrawl scrape https://example.com -o .firecrawl/example.md. firecrawl scrape https://example.com --html -o .firecrawl/example.html. firecrawl scrape https://example.com --format markdown,links -o .firecrawl/example.json. firecrawl scrape https://example.com --only-main-content -o .firecrawl/example.md. firecrawl crawl https://example.com. firecrawl crawl https://example.com --wait -o crawl-results.json --pretty. firecrawl map https://example.com --json -o .firecrawl/urls.json.\n\n4. CLI & Skill - Firecrawl - Mintlify\n   URL: https://mintlify.com/firecrawl/firecrawl/sdks/cli\n   Install the Firecrawl skill to enable CLI and AI agent integration: npx skills add firecrawl/cli. After installation, restart your AI agent to activate the\n\n5. Generate Claude Code skills from any documentation URL - DEV Community\n   URL: https://dev.to/jonesrussell/generate-claude-code-skills-from-any-documentation-url-3ao\n   # Generate Claude Code skills from any documentation URL. The Firecrawl plugin includes a skill-gen command that scrapes a documentation URL and generates a complete skill file from it. This post covers how to install the plugin, generate your first skill, and refine it into something worth keeping. The one you want is `skill-gen`. ## Generate a Skill. For example, to generate a skill for Taskfile:. The output lands in `~/.claude/skills/<skill-name>/SKILL.md`. Below the frontmatter you get sections covering when to use the skill, core patterns from the documentation, common mistakes, and verification steps. If you generated a skill from React\'s docs, you\'d want to narrow it to the specific patterns your project uses, not all of React. A good skill tells Claude when to use it and when not to. If you\'re working with a new framework or API and want Claude to have accurate, current knowledge of it, generate a skill and refine it.', artifact_id=None, error_type=None, error_message=None, duration_ms=0, metadata={'results': [{'title': 'firecrawl/firecrawl-scrape — Agent Skills | officialskills.sh', 'url': 'https://officialskills.sh/firecrawl/skills/firecrawl-scrape', 'content': "# firecrawl-scrape. Extracts clean markdown from any URL, including JavaScript-rendered single-page apps. ## Setup & Installation. npx skills add https://github.com/firecrawl/firecrawl-scrape --skill firecrawl-scrape. or paste the link and ask your coding assistant to install it. https://github.com/firecrawl/firecrawl-scrape. ## What This Skill Does. Extracts clean markdown from any URL, including JavaScript-rendered single-page apps. Supports multiple concurrent URLs and returns content optimized for LLM consumption. Unlike WebFetch, it handles JS-rendered SPAs and scrapes multiple URLs in parallel, so you get usable markdown instead of raw HTML or incomplete content. * Pulling pricing page content from a SaaS site to compare plans. * Extracting docs from a JS-rendered framework site that WebFetch can't read. * Scraping multiple product pages concurrently into local markdown files. * Asking a targeted question about a specific webpage without saving the full content. ## Similar Skills. ### pptx-generator. Generates, edits, and reads PowerPoint files using PptxGenJS and markitdown.### wp-project-triage. Writes internal communications in company-specific formats.### pptx.", 'score': 0.843393}, {'title': 'cli/skills/firecrawl-scrape/SKILL.md at main · firecrawl/cli · GitHub', 'url': 'https://github.com/firecrawl/cli/blob/main/skills/firecrawl-scrape/SKILL.md', 'content': 'You signed in with another tab or window. | description | Extract clean markdown from any URL, including JavaScript-rendered SPAs. Use this skill whenever the user provides a URL and wants its content, says "scrape", "grab", "fetch", "pull", "get the page", "extract from this URL", or "read this webpage". Handles JS-rendered pages, multiple concurrent URLs, and returns LLM-optimized markdown. # Basic markdown extraction # "" " "# Main content only, no nav/footer # "" " "# Wait for JS to render, then scrape # "" " "# Multiple URLs (each saved to .firecrawl/) # # Get markdown and links together # "" " " # Ask a question about the page #"https://example.com/pricing" " " "What is the enterprise plan price?" " ". * **Prefer plain scrape over `--query`.** Scrape to a file, then use `grep`, `head`, or read the markdown directly — you can search and reason over the full content yourself. * firecrawl-interact — when scrape can\'t get the content, use `interact` to click, fill forms, etc.', 'score': 0.7500592}, {'title': 'Firecrawl Skills — AI Skill — Termo', 'url': 'https://termo.ai/skills/firecrawl-skills', 'content': '## Launch an agent with Firecrawl Skills. # Firecrawl Skills. Firecrawl CLI for web scraping, crawling, and search. Scrape single pages or entire websites, map site URLs, and search the web with full content extraction. Use the `firecrawl` CLI to fetch and search the web. firecrawl search "your query" -o .firecrawl/search-{query}.json. firecrawl search "your query" --scrape -o .firecrawl/search-{query}-scraped.json. firecrawl scrape https://example.com -o .firecrawl/{site}-{path}.md. firecrawl search "your query" -o .firecrawl/search-query.txt. firecrawl search "your query" -o .firecrawl/search-query.json --json. firecrawl search "AI news" --limit 10 -o .firecrawl/search-ai-news.json --json. firecrawl search "tech startups" --sources news -o .firecrawl/search-news.json --json. firecrawl search "machine learning" --sources web,news,images -o .firecrawl/search-ml.json --json. firecrawl search "web scraping python" --categories github -o .firecrawl/search-github.json --json. firecrawl search "firecrawl tutorials" --scrape -o .firecrawl/search-scraped.json --json. firecrawl scrape https://example.com -o .firecrawl/example.md. firecrawl scrape https://example.com --html -o .firecrawl/example.html. firecrawl scrape https://example.com --format markdown,links -o .firecrawl/example.json. firecrawl scrape https://example.com --only-main-content -o .firecrawl/example.md. firecrawl crawl https://example.com. firecrawl crawl https://example.com --wait -o crawl-results.json --pretty. firecrawl map https://example.com --json -o .firecrawl/urls.json.', 'score': 0.6450534}, {'title': 'CLI & Skill - Firecrawl - Mintlify', 'url': 'https://mintlify.com/firecrawl/firecrawl/sdks/cli', 'content': 'Install the Firecrawl skill to enable CLI and AI agent integration: npx skills add firecrawl/cli. After installation, restart your AI agent to activate the', 'score': 0.6334335}, {'title': 'Generate Claude Code skills from any documentation URL - DEV Community', 'url': 'https://dev.to/jonesrussell/generate-claude-code-skills-from-any-documentation-url-3ao', 'content': "# Generate Claude Code skills from any documentation URL. The Firecrawl plugin includes a skill-gen command that scrapes a documentation URL and generates a complete skill file from it. This post covers how to install the plugin, generate your first skill, and refine it into something worth keeping. The one you want is `skill-gen`. ## Generate a Skill. For example, to generate a skill for Taskfile:. The output lands in `~/.claude/skills/<skill-name>/SKILL.md`. Below the frontmatter you get sections covering when to use the skill, core patterns from the documentation, common mistakes, and verification steps. If you generated a skill from React's docs, you'd want to narrow it to the specific patterns your project uses, not all of React. A good skill tells Claude when to use it and when not to. If you're working with a new framework or API and want Claude to have accurate, current knowledge of it, generate a skill and refine it.", 'score': 0.5683445}], 'answer': None, 'response_time': 0.88})