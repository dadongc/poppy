ToolResult(call_id='', name='web_search', status='ok', content='1. firecrawl/firecrawl-scrape — Agent Skills | officialskills.sh\n   URL: https://officialskills.sh/firecrawl/skills/firecrawl-scrape\n   # firecrawl-scrape. Extracts clean markdown from any URL, including JavaScript-rendered single-page apps. ## Setup & Installation. npx skills add https://github.com/firecrawl/firecrawl-scrape --skill firecrawl-scrape. or paste the link and ask your coding assistant to install it. https://github.com/firecrawl/firecrawl-scrape. ## What This Skill Does. Extracts clean markdown from any URL, including JavaScript-rendered single-page apps. Supports multiple concurrent URLs and returns content optimized for LLM consumption. Unlike WebFetch, it handles JS-rendered SPAs and scrapes multiple URLs in parallel, so you get usable markdown instead of raw HTML or incomplete content. * Pulling pricing page content from a SaaS site to compare plans. * Extracting docs from a JS-rendered framework site that WebFetch can\'t read. * Scraping multiple product pages concurrently into local markdown files. * Asking a targeted question about a specific webpage without saving the full content. ## Similar Skills. ### pptx-generator. Generates, edits, and reads PowerPoint files using PptxGenJS and markitdown.### wp-project-triage. Writes internal communications in company-specific formats.### pptx.\n\n2. cli/skills/firecrawl-crawl/SKILL.md at main - GitHub\n   URL: https://github.com/firecrawl/cli/blob/main/skills/firecrawl-crawl/SKILL.md\n   Use this skill when the user wants to crawl a site, extract all pages from a docs section, bulk-scrape multiple pages following links, or says "crawl", "get all\n\n3. cli/skills/firecrawl-scrape/SKILL.md at main - GitHub\n   URL: https://github.com/firecrawl/cli/blob/main/skills/firecrawl-scrape/SKILL.md\n   You signed in with another tab or window. | description | Extract clean markdown from any URL, including JavaScript-rendered SPAs. Use this skill whenever the user provides a URL and wants its content, says "scrape", "grab", "fetch", "pull", "get the page", "extract from this URL", or "read this webpage". Handles JS-rendered pages, multiple concurrent URLs, and returns LLM-optimized markdown. # Basic markdown extraction # "" " "# Main content only, no nav/footer # "" " "# Wait for JS to render, then scrape # "" " "# Multiple URLs (each saved to .firecrawl/) # # Get markdown and links together # "" " " # Ask a question about the page #"https://example.com/pricing" " " "What is the enterprise plan price?" " ". * **Prefer plain scrape over `--query`.** Scrape to a file, then use `grep`, `head`, or read the markdown directly — you can search and reason over the full content yourself. * firecrawl-interact — when scrape can\'t get the content, use `interact` to click, fill forms, etc.\n\n4. Firecrawl/firecrawl-build-scrape — Agent Skills | officialskills.sh\n   URL: https://officialskills.sh/firecrawl/skills/firecrawl-build-scrape\n   # firecrawl-build-scrape. Integrate Firecrawl `/scrape` for single-page extraction to markdown, HTML, links, screenshots, or structured output. npx skills add https://github.com/firecrawl/skills --skill firecrawl-build-scrape. or paste the link and ask your coding assistant to install it. https://github.com/firecrawl/skills/tree/main/skills/firecrawl-build-scrape. ## What This Skill Does. Builds page-level extraction with Firecrawl `/scrape` when an app already has a URL and needs markdown, HTML, links, screenshots, metadata, or structured page output. Preferred over broader crawl patterns when the feature is page-level. Focuses on the page-level case so integrations do not over-reach into crawling when they just need one URL turned into usable content. * Extracting markdown from a product URL inside an app flow. * Pulling metadata and links from a known page for indexing. * Running structured extraction against a single page\'s schema. * Converting a JS-rendered URL into clean content from product code. ## Similar Skills. ### pptx-generator. Inspects a WordPress repository and outputs a structured JSON report describing the project kind, tooling, tests, and version hints.### internal-comms.\n\n5. README.md - BexTuychiev/firecrawl-claude-code-skill - GitHub\n   URL: https://github.com/BexTuychiev/firecrawl-claude-code-skill/blob/main/README.md?plain=1\n   A Claude Code skill for web scraping with Firecrawl - markdown extraction, screenshots, structured data, web search, and doc crawling', artifact_id=None, error_type=None, error_message=None, duration_ms=0, metadata={'results': [{'title': 'firecrawl/firecrawl-scrape — Agent Skills | officialskills.sh', 'url': 'https://officialskills.sh/firecrawl/skills/firecrawl-scrape', 'content': "# firecrawl-scrape. Extracts clean markdown from any URL, including JavaScript-rendered single-page apps. ## Setup & Installation. npx skills add https://github.com/firecrawl/firecrawl-scrape --skill firecrawl-scrape. or paste the link and ask your coding assistant to install it. https://github.com/firecrawl/firecrawl-scrape. ## What This Skill Does. Extracts clean markdown from any URL, including JavaScript-rendered single-page apps. Supports multiple concurrent URLs and returns content optimized for LLM consumption. Unlike WebFetch, it handles JS-rendered SPAs and scrapes multiple URLs in parallel, so you get usable markdown instead of raw HTML or incomplete content. * Pulling pricing page content from a SaaS site to compare plans. * Extracting docs from a JS-rendered framework site that WebFetch can't read. * Scraping multiple product pages concurrently into local markdown files. * Asking a targeted question about a specific webpage without saving the full content. ## Similar Skills. ### pptx-generator. Generates, edits, and reads PowerPoint files using PptxGenJS and markitdown.### wp-project-triage. Writes internal communications in company-specific formats.### pptx.", 'score': 0.8296166}, {'title': 'cli/skills/firecrawl-crawl/SKILL.md at main - GitHub', 'url': 'https://github.com/firecrawl/cli/blob/main/skills/firecrawl-crawl/SKILL.md', 'content': 'Use this skill when the user wants to crawl a site, extract all pages from a docs section, bulk-scrape multiple pages following links, or says "crawl", "get all', 'score': 0.7946564}, {'title': 'cli/skills/firecrawl-scrape/SKILL.md at main - GitHub', 'url': 'https://github.com/firecrawl/cli/blob/main/skills/firecrawl-scrape/SKILL.md', 'content': 'You signed in with another tab or window. | description | Extract clean markdown from any URL, including JavaScript-rendered SPAs. Use this skill whenever the user provides a URL and wants its content, says "scrape", "grab", "fetch", "pull", "get the page", "extract from this URL", or "read this webpage". Handles JS-rendered pages, multiple concurrent URLs, and returns LLM-optimized markdown. # Basic markdown extraction # "" " "# Main content only, no nav/footer # "" " "# Wait for JS to render, then scrape # "" " "# Multiple URLs (each saved to .firecrawl/) # # Get markdown and links together # "" " " # Ask a question about the page #"https://example.com/pricing" " " "What is the enterprise plan price?" " ". * **Prefer plain scrape over `--query`.** Scrape to a file, then use `grep`, `head`, or read the markdown directly — you can search and reason over the full content yourself. * firecrawl-interact — when scrape can\'t get the content, use `interact` to click, fill forms, etc.', 'score': 0.77364457}, {'title': 'Firecrawl/firecrawl-build-scrape — Agent Skills | officialskills.sh', 'url': 'https://officialskills.sh/firecrawl/skills/firecrawl-build-scrape', 'content': "# firecrawl-build-scrape. Integrate Firecrawl `/scrape` for single-page extraction to markdown, HTML, links, screenshots, or structured output. npx skills add https://github.com/firecrawl/skills --skill firecrawl-build-scrape. or paste the link and ask your coding assistant to install it. https://github.com/firecrawl/skills/tree/main/skills/firecrawl-build-scrape. ## What This Skill Does. Builds page-level extraction with Firecrawl `/scrape` when an app already has a URL and needs markdown, HTML, links, screenshots, metadata, or structured page output. Preferred over broader crawl patterns when the feature is page-level. Focuses on the page-level case so integrations do not over-reach into crawling when they just need one URL turned into usable content. * Extracting markdown from a product URL inside an app flow. * Pulling metadata and links from a known page for indexing. * Running structured extraction against a single page's schema. * Converting a JS-rendered URL into clean content from product code. ## Similar Skills. ### pptx-generator. Inspects a WordPress repository and outputs a structured JSON report describing the project kind, tooling, tests, and version hints.### internal-comms.", 'score': 0.75027883}, {'title': 'README.md - BexTuychiev/firecrawl-claude-code-skill - GitHub', 'url': 'https://github.com/BexTuychiev/firecrawl-claude-code-skill/blob/main/README.md?plain=1', 'content': 'A Claude Code skill for web scraping with Firecrawl - markdown extraction, screenshots, structured data, web search, and doc crawling', 'score': 0.71113056}], 'answer': None, 'response_time': 0.76})