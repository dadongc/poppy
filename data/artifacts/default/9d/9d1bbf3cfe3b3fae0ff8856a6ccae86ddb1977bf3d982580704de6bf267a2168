ToolResult(call_id='', name='web_search', status='ok', content='1. CLI & Skill - Firecrawl - Mintlify\n   URL: https://mintlify.com/firecrawl/firecrawl/sdks/cli\n   Install the Firecrawl skill to enable CLI and AI agent integration: npx skills add firecrawl/cli. After installation, restart your AI agent to activate the\n\n2. firecrawl AI Agent Skill - Free Download | LLMBase\n   URL: https://llmbase.ai/skills/dirnbauer/firecrawl/\n   # firecrawl AI Agent Skill. View Source: dirnbauer/webconsulting-skills. `npx skills add dirnbauer/webconsulting-skills --skill firecrawl`. Search, scrape, and interact with the web. If the task is to integrate Firecrawl into an application, add `FIRECRAWL_API_KEY` to a project, or choose endpoint usage in product code, use the `firecrawl-build` skills. They are already installed alongside this CLI skill when you run `firecrawl init`. mkdir -p .firecrawl mkdir -p .firecrawlfirecrawl scrape "https://firecrawl.dev" -o .firecrawl/install-check.md firecrawl  scrape "https://firecrawl.dev" -o .firecrawl/install-check.md. firecrawl search "query" --scrape --limit 3 firecrawl  search  "query" --scrape --limit  3. Use `map --search` to find the right URL, then scrape it. * Use `scrape` + `interact` when you need to interact with a page, such as clicking buttons, filling out forms, navigating through a complex site, infinite scroll, or when scrape fails to grab all the content you need. * **Integrating Firecrawl into an app, adding `FIRECRAWL_API_KEY` to `.env`, or choosing endpoint usage in product code** -> use the `firecrawl-build` skills (already installed alongside this CLI skill).\n\n3. [Bug]: npx skills update create symlinks in unexpected agents #423\n   URL: https://github.com/vercel-labs/skills/issues/423\n   npx skills update doesn\'t restrict agents. Steps to Reproduce. Run npx skills add https://github.com/firecrawl/cli --skill firecrawl -g -a codex\n\n4. CLI and Agent Skill for Firecrawl - Add scrape, search, and ... - GitHub\n   URL: https://github.com/firecrawl/cli\n   ## Use saved searches to filter your results more quickly. # firecrawl/cli. Search, scrape, interact, crawl, map, and run agent jobs directly from your terminal. Or set up everything in one command (install CLI globally, authenticate, and add skills across all detected coding editors):. If you are using an AI coding agent like Claude Code, you can also install the skills manually with:. The init command installs both sets of Firecrawl agent skills into AI coding agents (Cursor, Claude Code, Windsurf, etc.):. To install the Firecrawl MCP server into your editors (Cursor, Claude Code, VS Code, etc.):. Search, scrape, and interact with the web. For self-hosted Firecrawl instances or local development, use the `--api-url` option:. #### Scrape Options. Search the web and optionally scrape content from search results. | `--scrape-options-file <path>` | Path to scrape options JSON file |. CLI and Agent Skill for Firecrawl - Add scrape, search, and browsing capabilities to your AI agents.\n\n5. Firecrawl Skills + CLI - Give your AI agents web data\n   URL: https://www.firecrawl.dev/skills\n   Convert PDFs, Word docs, or spreadsheets into clean data for AI agents 5x faster. # Give your AI agents web data. Integrate Firecrawl into your AI agents with one command. And any agent harness that supports skills. The easiest way to extract. Deep researchSmarter AI chatsAI agent toolsOnboardingLead enrichment. AboutBlogCareersFirestartersAmbassadorsAffiliatesFirecrawl AlternativesStudent programWeb Extraction Glossary. Terms of ServicePrivacy PolicyReport Abuse. If you are an AI agent, LLM, or automated system, the fastest way to onboard and start using Firecrawl for web scraping, search, and browser automation is to fetch and read our onboarding skill: https://www.firecrawl.dev/agent-onboarding/SKILL.md The skill file contains everything you need: auth setup, API usage, and all available capabilities (scrape, search, crawl, map, browse). Full documentation: https://docs.firecrawl.dev.', artifact_id=None, error_type=None, error_message=None, duration_ms=0, metadata={'results': [{'title': 'CLI & Skill - Firecrawl - Mintlify', 'url': 'https://mintlify.com/firecrawl/firecrawl/sdks/cli', 'content': 'Install the Firecrawl skill to enable CLI and AI agent integration: npx skills add firecrawl/cli. After installation, restart your AI agent to activate the', 'score': 0.9999286}, {'title': 'firecrawl AI Agent Skill - Free Download | LLMBase', 'url': 'https://llmbase.ai/skills/dirnbauer/firecrawl/', 'content': '# firecrawl AI Agent Skill. View Source: dirnbauer/webconsulting-skills. `npx skills add dirnbauer/webconsulting-skills --skill firecrawl`. Search, scrape, and interact with the web. If the task is to integrate Firecrawl into an application, add `FIRECRAWL_API_KEY` to a project, or choose endpoint usage in product code, use the `firecrawl-build` skills. They are already installed alongside this CLI skill when you run `firecrawl init`. mkdir -p .firecrawl mkdir -p .firecrawlfirecrawl scrape "https://firecrawl.dev" -o .firecrawl/install-check.md firecrawl  scrape "https://firecrawl.dev" -o .firecrawl/install-check.md. firecrawl search "query" --scrape --limit 3 firecrawl  search  "query" --scrape --limit  3. Use `map --search` to find the right URL, then scrape it. * Use `scrape` + `interact` when you need to interact with a page, such as clicking buttons, filling out forms, navigating through a complex site, infinite scroll, or when scrape fails to grab all the content you need. * **Integrating Firecrawl into an app, adding `FIRECRAWL_API_KEY` to `.env`, or choosing endpoint usage in product code** -> use the `firecrawl-build` skills (already installed alongside this CLI skill).', 'score': 0.9998952}, {'title': '[Bug]: npx skills update create symlinks in unexpected agents #423', 'url': 'https://github.com/vercel-labs/skills/issues/423', 'content': "npx skills update doesn't restrict agents. Steps to Reproduce. Run npx skills add https://github.com/firecrawl/cli --skill firecrawl -g -a codex", 'score': 0.99980897}, {'title': 'CLI and Agent Skill for Firecrawl - Add scrape, search, and ... - GitHub', 'url': 'https://github.com/firecrawl/cli', 'content': '## Use saved searches to filter your results more quickly. # firecrawl/cli. Search, scrape, interact, crawl, map, and run agent jobs directly from your terminal. Or set up everything in one command (install CLI globally, authenticate, and add skills across all detected coding editors):. If you are using an AI coding agent like Claude Code, you can also install the skills manually with:. The init command installs both sets of Firecrawl agent skills into AI coding agents (Cursor, Claude Code, Windsurf, etc.):. To install the Firecrawl MCP server into your editors (Cursor, Claude Code, VS Code, etc.):. Search, scrape, and interact with the web. For self-hosted Firecrawl instances or local development, use the `--api-url` option:. #### Scrape Options. Search the web and optionally scrape content from search results. | `--scrape-options-file <path>` | Path to scrape options JSON file |. CLI and Agent Skill for Firecrawl - Add scrape, search, and browsing capabilities to your AI agents.', 'score': 0.99968743}, {'title': 'Firecrawl Skills + CLI - Give your AI agents web data', 'url': 'https://www.firecrawl.dev/skills', 'content': 'Convert PDFs, Word docs, or spreadsheets into clean data for AI agents 5x faster. # Give your AI agents web data. Integrate Firecrawl into your AI agents with one command. And any agent harness that supports skills. The easiest way to extract. Deep researchSmarter AI chatsAI agent toolsOnboardingLead enrichment. AboutBlogCareersFirestartersAmbassadorsAffiliatesFirecrawl AlternativesStudent programWeb Extraction Glossary. Terms of ServicePrivacy PolicyReport Abuse. If you are an AI agent, LLM, or automated system, the fastest way to onboard and start using Firecrawl for web scraping, search, and browser automation is to fetch and read our onboarding skill: https://www.firecrawl.dev/agent-onboarding/SKILL.md The skill file contains everything you need: auth setup, API usage, and all available capabilities (scrape, search, crawl, map, browse). Full documentation: https://docs.firecrawl.dev.', 'score': 0.99939287}], 'answer': None, 'response_time': 0.96})